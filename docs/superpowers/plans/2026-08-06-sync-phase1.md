# 동기화 1단계(업로드) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 아이패드의 기록이 Supabase에 단방향으로 쌓이게 한다 — pull 없음, 병합 없음. 파괴적
경로(초기화·가져오기)는 서버 스냅샷 뒤에만 진행된다.

**Architecture:** 설계 `docs/superpowers/specs/2026-08-06-sync-backend-design.md`의 「배포 단계」
1단계. `putDay`/`putMeta`가 같은 IndexedDB 트랜잭션으로 아웃박스에 표식을 남기고,
`src/data/sync.ts`가 그것을 PostgREST로 밀어낸다. 화면은 동기화를 모른다. 순수 로직(접기·상태
판정)은 `engine/`에 두고 테스트한다.

**Tech Stack:** 바닐라 TS + `fetch` 직접 호출(PostgREST). **`supabase-js`를 설치하지 않는다** —
실행 의존성 0개 유지가 설계 요구다.

## Global Constraints

- 모든 npm 명령 전: `export PATH="$HOME/.local/share/mise/installs/node/lts/bin:$PATH"`
- **브랜치 `sync-phase1`에서 작업한다.** 여러 커밋짜리 기능이라 main 직접이 아니다. 중간
  커밋은 깨져도 되고, 브랜치 push는 배포를 유발하지 않는다
- 커밋 전 `npm run format` (docs 포함 — CI의 `prettier --check .`가 마크다운까지 본다)
- `git add <명시 경로>`만. `git add .` 금지
- 테스트는 `engine/`과 `data/db.test.ts`에만. DOM·화면·네트워크 조율은 테스트하지 않는다
  (설계 §12). 새 순수 로직마다 **변이 검증**(구현을 틀리게 바꿔 그 테스트만 빨개지는지) 습관
- 서버 유래 값이 `el()` 템플릿에 들어가면 `escapeHtml` 필수
- 문서·코드에 실제 Supabase URL·키를 적지 않는다. 플레이스홀더만 (`sync-config.ts`는 사람이
  로컬에서 채우고 커밋한다 — URL·anon 키는 공개 전제라 커밋 가능)
- `Meta.derived`를 읽거나 채우는 코드를 만들지 않는다
- 스키마·프로토콜 근거가 필요하면 설계 문서를 연다. 이 계획과 어긋나면 **설계 문서가 맞다**

## 파일 구조

| 파일                                  | 책임                                                                  |
| ------------------------------------- | --------------------------------------------------------------------- |
| `supabase/schema.sql`                 | 서버 스키마 전체(테이블·RLS·트리거·RPC). **서버 규칙의 단일 출처**    |
| `supabase/README.md`                  | 아빠가 손으로 하는 단계(프로젝트 생성·스키마 적용·기기 등록 SQL·확인) |
| `src/engine/outbox.ts`                | `SyncBundle`·`OutboxEntry` 타입, `foldOutbox` 순수 함수               |
| `src/engine/sync-status.ts`           | 부모 홈 상태줄 문구 판정 순수 함수                                    |
| `src/data/db.ts`                      | DB v2: `outbox`·`device` 스토어, `putDay(day, changed)` 표식          |
| `src/data/sync-config.ts`             | `SUPABASE_URL`·`SUPABASE_ANON_KEY` 상수(비면 동기화 꺼짐)             |
| `src/data/sync.ts`                    | push 엔진, 스냅샷·`replace_all`·복원 RPC 호출. 얇게 유지              |
| `src/ui.ts`                           | `confirmDialog`에 `requireText` 옵션                                  |
| `src/screens/home-parent.ts`          | 미등록 안내 + 키 등록 폼 + 동기화 상태줄                              |
| `src/screens/report.ts`               | §6 흐름(스냅샷→"지우기" 입력→로컬→서버) + 스냅샷 되돌리기             |
| `src/main.ts`                         | push 트리거 배선(시작·`visibilitychange`·아웃박스 이벤트)             |
| `.github/workflows/ping-supabase.yml` | 주 1회 ping으로 무료 티어 일시정지 예방                               |

---

### Task 1: 서버 스키마와 사람 절차서

**Files:**

- Create: `supabase/schema.sql`
- Create: `supabase/README.md`

**Interfaces:**

- Produces: 테이블 `days`·`meta`·`app_config`·`devices`·`snapshots`·`write_log`, 함수
  `haruchi_device()`, RPC `replace_all(p_payload jsonb)`·`rewrite_sheet(p_date text, p_payload jsonb, p_rev bigint)`.
  이후 태스크의 모든 REST 경로가 이 이름을 쓴다

- [ ] **Step 1: `supabase/schema.sql` 작성**

```sql
-- haruchi 동기화 백엔드 스키마. 단일 출처 — 서버 규칙을 바꿀 때는 이 파일을 고치고
-- Supabase SQL Editor에서 재적용한다. 근거: docs/superpowers/specs/2026-08-06-sync-backend-design.md
create extension if not exists pgcrypto;

create table if not exists devices (
  id           text primary key,
  label        text not null,
  key_hash     text not null,
  created_at   timestamptz not null default now(),
  last_seen_at timestamptz,
  revoked_at   timestamptz
);

create table if not exists days (
  date           text primary key,
  payload        jsonb not null,
  rev            bigint not null,
  schema_version int not null,
  sheet_at       timestamptz,
  grades_at      timestamptz,
  sprint_at      timestamptz,
  updated_at     timestamptz not null default now(),
  device         text not null
);

create table if not exists meta (
  id         int primary key default 1 check (id = 1),
  payload    jsonb not null,
  rev        bigint not null,
  generation bigint not null,
  updated_at timestamptz not null default now(),
  device     text not null
);

create table if not exists app_config (
  id         int primary key default 1 check (id = 1),
  pin        text not null,
  updated_at timestamptz not null default now(),
  device     text not null
);

create table if not exists snapshots (
  id        bigserial primary key,
  at        timestamptz not null default now(),
  device    text not null,
  reason    text not null,
  day_count int not null,
  payload   jsonb not null
);

create table if not exists write_log (
  id     bigserial primary key,
  at     timestamptz not null default now(),
  device text not null,
  target text not null,
  action text not null
);

-- meta는 행이 항상 존재해야 한다 — generation 카운터가 행과 함께 영속한다(설계 §6).
insert into meta (id, payload, rev, generation, device)
  values (1, '{}'::jsonb, 0, 0, 'schema')
  on conflict (id) do nothing;

-- 요청 헤더의 기기 키를 확인해 기기 id를 돌려준다. RLS·트리거 공용.
create or replace function haruchi_device() returns text
language sql stable security definer as $$
  select d.id from devices d
  where d.key_hash = crypt(
    coalesce(current_setting('request.headers', true)::json->>'x-device-key', ''),
    d.key_hash)
    and d.revoked_at is null
$$;

-- updated_at은 서버 시계다(pull 커서용, 설계 §1). *_at(기기 시계)과 역할이 다르다.
create or replace function haruchi_touch() returns trigger
language plpgsql as $$
begin
  new.updated_at := now();
  return new;
end $$;

create trigger days_touch before insert or update on days
  for each row execute function haruchi_touch();
create trigger meta_touch before insert or update on meta
  for each row execute function haruchi_touch();
create trigger config_touch before insert or update on app_config
  for each row execute function haruchi_touch();

-- sheet 불변(설계 §2 C-1 방어 2). 비어 있지 않은 sheet는 rewrite_sheet RPC 밖에서 못 바꾼다.
create or replace function haruchi_guard_sheet() returns trigger
language plpgsql as $$
begin
  if coalesce(old.payload->'sheet', '[]'::jsonb) <> '[]'::jsonb
     and new.payload->'sheet' is distinct from old.payload->'sheet'
     and coalesce(current_setting('haruchi.sheet_rewrite', true), '') <> 'on' then
    raise exception 'sheet_immutable';
  end if;
  return new;
end $$;

create trigger days_guard_sheet before update on days
  for each row execute function haruchi_guard_sheet();

-- write_log 자동 기록 + last_seen_at 갱신. 클라이언트 추가 요청 없이 서버가 남긴다.
create or replace function haruchi_log() returns trigger
language plpgsql as $$
begin
  insert into write_log (device, target, action)
    values (new.device, 'day:' || new.date, lower(TG_OP));
  update devices set last_seen_at = now() where id = new.device;
  return new;
end $$;

create trigger days_log after insert or update on days
  for each row execute function haruchi_log();

-- 파괴적 쓰기의 유일한 경로(설계 §6). 한 트랜잭션: 자동 스냅샷 → days 교체 → meta 갱신.
-- meta 행은 지우지 않는다. p_payload 형식: { "days": Day[], "meta": Meta }
create or replace function replace_all(p_payload jsonb) returns void
language plpgsql security definer as $$
declare
  dev text := haruchi_device();
begin
  if dev is null then raise exception 'unauthorized'; end if;
  insert into snapshots (device, reason, day_count, payload)
    select dev, 'auto', (select count(*) from days),
      jsonb_build_object('days', coalesce(jsonb_agg(d.payload), '[]'::jsonb),
                         'meta', (select payload from meta where id = 1))
    from days d;
  delete from days;
  insert into days (date, payload, rev, schema_version, device)
    select day->>'date', day, 1, 1, dev
    from jsonb_array_elements(coalesce(p_payload->'days', '[]'::jsonb)) as day;
  update meta set payload = coalesce(p_payload->'meta', '{}'::jsonb),
    rev = rev + 1, generation = generation + 1, device = dev where id = 1;
  insert into write_log (device, target, action) values (dev, 'all', 'replace_all');
end $$;

-- 다시 만들기 전용(설계 §2 C-1). 채점이 있는 날은 거부 — 지금 클라이언트와 같은 조건.
create or replace function rewrite_sheet(p_date text, p_payload jsonb, p_rev bigint) returns void
language plpgsql security definer as $$
declare
  dev text := haruchi_device();
begin
  if dev is null then raise exception 'unauthorized'; end if;
  if exists (select 1 from days where date = p_date
             and coalesce(payload->'grades', '{}'::jsonb) <> '{}'::jsonb) then
    raise exception 'sheet_rewrite_graded';
  end if;
  perform set_config('haruchi.sheet_rewrite', 'on', true);
  update days set payload = p_payload, rev = p_rev, sheet_at = now(), device = dev
    where date = p_date and rev = p_rev - 1;
  if not found then raise exception 'rev_conflict'; end if;
  insert into write_log (device, target, action) values (dev, 'day:' || p_date, 'sheet-rewrite');
end $$;

-- RLS: 키가 맞는 기기에게만 전부, 아니면 전무. devices 테이블 자체는 정책이 없어
-- 클라이언트가 못 읽는다(관리는 대시보드에서).
alter table days enable row level security;
alter table meta enable row level security;
alter table app_config enable row level security;
alter table snapshots enable row level security;
alter table write_log enable row level security;
alter table devices enable row level security;

create policy days_all on days for all
  using (haruchi_device() is not null) with check (haruchi_device() is not null);
create policy meta_all on meta for all
  using (haruchi_device() is not null) with check (haruchi_device() is not null);
create policy config_all on app_config for all
  using (haruchi_device() is not null) with check (haruchi_device() is not null);
create policy snapshots_rw on snapshots for all
  using (haruchi_device() is not null) with check (haruchi_device() is not null);
create policy log_rw on write_log for all
  using (haruchi_device() is not null) with check (haruchi_device() is not null);
```

- [ ] **Step 2: `supabase/README.md` 작성** — 아빠가 하는 절차. 실제 값은 적지 않는다

```markdown
# Supabase 설정 절차 (사람이 한다)

1. https://supabase.com 에서 프로젝트 생성 — 리전 Seoul(ap-northeast-2), 무료 티어
2. SQL Editor에 `schema.sql` 전체를 붙여 넣고 실행
3. 기기 키 발급 (기기마다 반복):
   - 비밀번호 관리자에서 32자 이상 랜덤 문자열 생성 → 관리자에 보관
   - 앱 부모 홈의 미등록 안내에 뜨는 기기 id를 확인
   - SQL Editor에서:
     `insert into devices (id, label, key_hash) values ('<기기id>', '아이패드', crypt('<랜덤키>', gen_salt('bf')));`
   - 앱의 키 입력란에 랜덤 키를 붙여 넣고 저장
4. `src/data/sync-config.ts`에 프로젝트 URL과 anon 키를 채워 커밋
   (Settings → API. **service_role은 절대 아니다** — 복사할 키 이름을 확인할 것)
5. `.github/workflows/ping-supabase.yml`의 URL·anon 키 플레이스홀더를 채워 커밋
6. 확인: 앱에서 문제지 인쇄 → Table Editor의 days에 오늘 행이 생기는지
7. 폐기가 필요하면: `update devices set revoked_at = now() where id = '<기기id>';`
```

- [ ] **Step 3: 검사 후 커밋**

```bash
npm run format && npx prettier --check .
git add supabase/schema.sql supabase/README.md
git commit -m "feat: 동기화 서버 스키마와 설정 절차서를 추가한다"
```

---

### Task 2: `engine/outbox.ts` — 표식 타입과 접기

**Files:**

- Create: `src/engine/outbox.ts`
- Test: `src/engine/outbox.test.ts`

**Interfaces:**

- Produces:
  - `type SyncBundle = 'sheet' | 'grades' | 'sprint'`
  - `type OutboxEntry = { target: string; bundleAt: Partial<Record<SyncBundle, string>>; at: string }`
    (`target`은 `'day:YYYY-MM-DD'` 또는 `'meta'`. `bundleAt`은 묶음별 기기 시계 ISO —
    설계 §3 "push는 bundles에 든 묶음의 \*\_at만 갱신한다"의 근거 데이터)
  - `foldOutbox(entries: OutboxEntry[]): OutboxEntry[]` — target별 하나로 접되 `bundleAt`은
    묶음별 최신값 합집합, `at`은 최댓값

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
import { describe, expect, it } from 'vitest'
import { foldOutbox } from './outbox'

describe('foldOutbox', () => {
  it('같은 target을 하나로 접고 bundleAt은 묶음별 최신값의 합집합이다', () => {
    const folded = foldOutbox([
      {
        target: 'day:2026-08-06',
        bundleAt: { sprint: '2026-08-06T10:00:00Z' },
        at: '2026-08-06T10:00:00Z',
      },
      {
        target: 'day:2026-08-06',
        bundleAt: { grades: '2026-08-06T11:00:00Z' },
        at: '2026-08-06T11:00:00Z',
      },
      {
        target: 'day:2026-08-06',
        bundleAt: { sprint: '2026-08-06T12:00:00Z' },
        at: '2026-08-06T12:00:00Z',
      },
    ])
    expect(folded).toEqual([
      {
        target: 'day:2026-08-06',
        bundleAt: { sprint: '2026-08-06T12:00:00Z', grades: '2026-08-06T11:00:00Z' },
        at: '2026-08-06T12:00:00Z',
      },
    ])
  })

  it('다른 target은 섞지 않고 입력 순서를 보존한다', () => {
    const folded = foldOutbox([
      {
        target: 'day:2026-08-05',
        bundleAt: { sheet: '2026-08-05T09:00:00Z' },
        at: '2026-08-05T09:00:00Z',
      },
      { target: 'meta', bundleAt: {}, at: '2026-08-05T09:30:00Z' },
    ])
    expect(folded.map((e) => e.target)).toEqual(['day:2026-08-05', 'meta'])
  })

  it('빈 입력은 빈 출력이다', () => {
    expect(foldOutbox([])).toEqual([])
  })
})
```

- [ ] **Step 2: 실패 확인** — `npx vitest run src/engine/outbox.test.ts` → 모듈 없음으로 FAIL
- [ ] **Step 3: 구현**

```ts
/**
 * 아웃박스 표식(설계 §3). payload를 담지 않는다 — push가 현재 값을 다시 읽는다.
 * bundleAt이 없으면 push가 어느 *_at을 갱신할지 알 수 없어 LWW가 무너진다(Fable 리뷰 2).
 */
export type SyncBundle = 'sheet' | 'grades' | 'sprint'

export type OutboxEntry = {
  target: string
  bundleAt: Partial<Record<SyncBundle, string>>
  at: string
}

/** target별 하나로 접는다. bundleAt은 묶음별 최신값 합집합 — 접기가 정보를 잃으면 안 된다. */
export function foldOutbox(entries: OutboxEntry[]): OutboxEntry[] {
  const byTarget = new Map<string, OutboxEntry>()
  for (const e of entries) {
    const cur = byTarget.get(e.target)
    if (!cur) {
      byTarget.set(e.target, { target: e.target, bundleAt: { ...e.bundleAt }, at: e.at })
      continue
    }
    for (const [bundle, at] of Object.entries(e.bundleAt) as [SyncBundle, string][]) {
      if (!cur.bundleAt[bundle] || cur.bundleAt[bundle] < at) cur.bundleAt[bundle] = at
    }
    if (cur.at < e.at) cur.at = e.at
  }
  return [...byTarget.values()]
}
```

- [ ] **Step 4: 통과 확인** — `npx vitest run src/engine/outbox.test.ts` → PASS
- [ ] **Step 5: 변이 검증** — 구현의 `cur.bundleAt[bundle] < at`를 `>`로 바꿔 첫 테스트만
      빨개지는지 확인 후 원복
- [ ] **Step 6: 커밋**

```bash
git add src/engine/outbox.ts src/engine/outbox.test.ts
git commit -m "feat: 아웃박스 표식 타입과 접기 함수를 추가한다"
```

---

### Task 3: `engine/sync-status.ts` — 부모 홈 상태줄 판정

**Files:**

- Create: `src/engine/sync-status.ts`
- Test: `src/engine/sync-status.test.ts`

**Interfaces:**

- Consumes: 없음 (순수)
- Produces: `syncStatus(input: { registered: boolean; outboxCount: number; lastSyncAt: string | null; today: string }): { tone: 'quiet' | 'warn' | 'setup'; lines: string[] }`
  - `registered: false` → `setup` + `['이 기기는 아직 연결되지 않았어요']`
  - 아웃박스 0 + 동기화 이력 있음 → `quiet` + `['마지막 동기화: …']`(오늘이면 `'오늘'`, N일 전이면 `'N일 전'`)
  - 아웃박스 > 0 → `warn` + `['아직 안 올라간 기록 N건']`
  - `lastSyncAt`이 3일 이상 과거 → `warn`에 `['서버에 N일째 못 올렸어요']` 추가
    (Supabase 일시정지가 이 줄로 드러난다 — 설계 「실패했을 때」 표)

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
import { describe, expect, it } from 'vitest'
import { syncStatus } from './sync-status'

describe('syncStatus', () => {
  it('미등록이면 setup 안내 한 줄이다', () => {
    expect(
      syncStatus({ registered: false, outboxCount: 0, lastSyncAt: null, today: '2026-08-06' }),
    ).toEqual({ tone: 'setup', lines: ['이 기기는 아직 연결되지 않았어요'] })
  })

  it('밀린 것이 없으면 조용한 한 줄이다', () => {
    expect(
      syncStatus({
        registered: true,
        outboxCount: 0,
        lastSyncAt: '2026-08-06T09:00:00Z',
        today: '2026-08-06',
      }),
    ).toEqual({ tone: 'quiet', lines: ['마지막 동기화: 오늘'] })
  })

  it('밀린 기록이 있으면 경고한다', () => {
    const s = syncStatus({
      registered: true,
      outboxCount: 3,
      lastSyncAt: '2026-08-04T09:00:00Z',
      today: '2026-08-06',
    })
    expect(s.tone).toBe('warn')
    expect(s.lines).toContain('아직 안 올라간 기록 3건')
  })

  it('3일 이상 서버에 못 닿으면 일수를 밝힌다', () => {
    const s = syncStatus({
      registered: true,
      outboxCount: 1,
      lastSyncAt: '2026-08-01T09:00:00Z',
      today: '2026-08-06',
    })
    expect(s.lines).toContain('서버에 5일째 못 올렸어요')
  })

  it('동기화 이력이 아예 없고 밀린 것도 없으면 경고하지 않는다(방금 등록한 기기)', () => {
    expect(
      syncStatus({ registered: true, outboxCount: 0, lastSyncAt: null, today: '2026-08-06' }),
    ).toEqual({ tone: 'quiet', lines: ['아직 동기화한 적이 없어요'] })
  })
})
```

- [ ] **Step 2: 실패 확인** — `npx vitest run src/engine/sync-status.test.ts` → FAIL
- [ ] **Step 3: 구현**

```ts
/** 부모 홈 동기화 상태줄(설계 §7 "평소에는 조용히"). 날짜 계산은 UTC 날짜 문자열 비교다. */
export function syncStatus(input: {
  registered: boolean
  outboxCount: number
  lastSyncAt: string | null
  today: string
}): { tone: 'quiet' | 'warn' | 'setup'; lines: string[] } {
  if (!input.registered) return { tone: 'setup', lines: ['이 기기는 아직 연결되지 않았어요'] }

  const lines: string[] = []
  let tone: 'quiet' | 'warn' = 'quiet'

  if (input.outboxCount > 0) {
    tone = 'warn'
    lines.push(`아직 안 올라간 기록 ${input.outboxCount}건`)
  }

  if (input.lastSyncAt === null) {
    if (tone === 'quiet') lines.push('아직 동기화한 적이 없어요')
  } else {
    const last = input.lastSyncAt.slice(0, 10)
    const days = Math.round((Date.parse(input.today) - Date.parse(last)) / (24 * 60 * 60 * 1000))
    if (days >= 3) {
      tone = 'warn'
      lines.push(`서버에 ${days}일째 못 올렸어요`)
    } else if (tone === 'quiet') {
      lines.push(`마지막 동기화: ${days <= 0 ? '오늘' : `${days}일 전`}`)
    }
  }

  return { tone, lines }
}
```

- [ ] **Step 4: 통과 확인** — `npx vitest run src/engine/sync-status.test.ts` → PASS
- [ ] **Step 5: 변이 검증** — `days >= 3`을 `days >= 30`으로 바꿔 넷째 테스트만 빨개지는지
      확인 후 원복
- [ ] **Step 6: 커밋**

```bash
git add src/engine/sync-status.ts src/engine/sync-status.test.ts
git commit -m "feat: 동기화 상태줄 판정 함수를 추가한다"
```

---

### Task 4: `db.ts` v2 — 아웃박스·기기 스토어와 표식 트랜잭션

**Files:**

- Modify: `src/data/db.ts`
- Modify: `src/screens/sprint.ts:226,256` · `src/screens/grade.ts:228` ·
  `src/screens/print-sheet.ts:145,236` (putDay 시그니처 변경 반영)
- Test: `src/data/db.test.ts`

**Interfaces:**

- Consumes: Task 2의 `SyncBundle`·`OutboxEntry`
- Produces:
  - `putDay(day: Day, changed: SyncBundle[]): Promise<void>` — **같은 트랜잭션**으로 아웃박스
    표식(`bundleAt`: 각 묶음에 지금 시각)
  - `putMeta(meta: Meta): Promise<void>` — target `'meta'` 표식(동일 트랜잭션)
  - `getOutbox(): Promise<(OutboxEntry & { key: number })[]>` (key 오름차순)
  - `deleteOutboxThrough(target: string, maxKey: number): Promise<void>` — **maxKey 이하만**
    지운다(설계 §3, Fable 리뷰 5)
  - `type DeviceState = { deviceId: string; deviceKey: string | null; lastSyncAt: string | null }`
  - `getDeviceState(): Promise<DeviceState>` — 없으면 `deviceId`를 `crypto.randomUUID().slice(0, 8)`로
    만들어 저장 후 반환
  - `putDeviceState(state: DeviceState): Promise<void>`
  - `replaceAll`은 아웃박스도 같은 트랜잭션에서 비운다(전체 교체 후 낡은 표식은 무의미).
    `device` 스토어는 **건드리지 않는다**
  - 커밋된 표식 뒤 `window.dispatchEvent(new Event('haruchi:outbox'))` (window 없으면 생략 —
    테스트 환경 가드)

- [ ] **Step 1: 실패하는 테스트 추가** — `src/data/db.test.ts`에 기존 스타일을 따라 붙인다
      (기존 테스트가 쓰는 fake-indexeddb 셋업을 그대로 사용)

```ts
describe('outbox', () => {
  it('putDay가 같은 트랜잭션으로 표식을 남긴다', async () => {
    await putDay({ date: '2026-08-06', kind: 'normal', sheet: [] }, ['sprint'])
    const entries = await getOutbox()
    expect(entries).toHaveLength(1)
    expect(entries[0]!.target).toBe('day:2026-08-06')
    expect(Object.keys(entries[0]!.bundleAt)).toEqual(['sprint'])
  })

  it('deleteOutboxThrough는 maxKey 이하만 지운다', async () => {
    await putDay({ date: '2026-08-06', kind: 'normal', sheet: [] }, ['sprint'])
    const [first] = await getOutbox()
    await putDay({ date: '2026-08-06', kind: 'normal', sheet: [] }, ['grades'])
    await deleteOutboxThrough('day:2026-08-06', first!.key)
    const rest = await getOutbox()
    expect(rest).toHaveLength(1)
    expect(Object.keys(rest[0]!.bundleAt)).toEqual(['grades'])
  })

  it('putMeta가 meta 표식을 남긴다', async () => {
    await putMeta(defaultMeta())
    const entries = await getOutbox()
    expect(entries.some((e) => e.target === 'meta')).toBe(true)
  })

  it('getDeviceState가 deviceId를 한 번만 만든다', async () => {
    const a = await getDeviceState()
    const b = await getDeviceState()
    expect(a.deviceId).toBe(b.deviceId)
    expect(a.deviceKey).toBeNull()
  })

  it('replaceAll이 아웃박스를 비우고 device 스토어는 남긴다', async () => {
    await putDeviceState({ deviceId: 'test', deviceKey: 'k', lastSyncAt: null })
    await putDay({ date: '2026-08-06', kind: 'normal', sheet: [] }, ['sprint'])
    await replaceAll([], defaultMeta())
    expect(await getOutbox()).toHaveLength(0)
    expect((await getDeviceState()).deviceKey).toBe('k')
  })
})
```

- [ ] **Step 2: 실패 확인** — `npx vitest run src/data/db.test.ts` → 새 테스트 FAIL
- [ ] **Step 3: 구현** — `DB_VERSION = 2`, `onupgradeneeded`에 스토어 추가:

```ts
const STORE_OUTBOX = 'outbox'
const STORE_DEVICE = 'device'
const DEVICE_KEY = 'current'
// onupgradeneeded 안:
if (!db.objectStoreNames.contains(STORE_OUTBOX)) {
  db.createObjectStore(STORE_OUTBOX, { autoIncrement: true })
}
if (!db.objectStoreNames.contains(STORE_DEVICE)) {
  db.createObjectStore(STORE_DEVICE)
}
```

`putDay`는 두 스토어를 한 트랜잭션으로:

```ts
/**
 * Day를 쓰고 같은 트랜잭션으로 아웃박스에 표식을 남긴다(설계 §3 — 쪼개면 "쓰기는 됐는데
 * 표식이 없어 영원히 안 올라가는 기록"이 생긴다). changed는 이번에 실제로 바꾼 묶음이다 —
 * push가 이 묶음의 *_at만 갱신한다. 빈 배열 금지(올릴 이유가 없는 쓰기는 없다).
 */
export function putDay(day: Day, changed: SyncBundle[]): Promise<void> {
  const at = new Date().toISOString()
  const bundleAt: OutboxEntry['bundleAt'] = {}
  for (const b of changed) bundleAt[b] = at
  return open().then(
    (db) =>
      new Promise<void>((resolve, reject) => {
        const tx = db.transaction([STORE_DAYS, STORE_OUTBOX], 'readwrite')
        tx.oncomplete = () => {
          notifyOutbox()
          resolve()
        }
        tx.onerror = () => reject(tx.error ?? new Error('IndexedDB 트랜잭션 실패'))
        tx.onabort = () => reject(tx.error ?? new Error('IndexedDB 트랜잭션 중단'))
        tx.objectStore(STORE_DAYS).put(day)
        tx.objectStore(STORE_OUTBOX).add({ target: `day:${day.date}`, bundleAt, at })
      }),
  )
}

/** 표식이 커밋된 뒤에만 알린다. 테스트(node) 환경 가드. */
function notifyOutbox(): void {
  if (typeof window !== 'undefined') window.dispatchEvent(new Event('haruchi:outbox'))
}
```

`putMeta`도 같은 꼴(`bundleAt: {}`, target `'meta'`). `getOutbox`는 `openCursor`로 key를
함께 모은다. `deleteOutboxThrough`는 커서로 돌며 `cursor.key <= maxKey && value.target === target`
만 `cursor.delete()`. `getDeviceState`/`putDeviceState`는 `STORE_DEVICE`의 `DEVICE_KEY` 한 칸.
`replaceAll`의 트랜잭션 배열에 `STORE_OUTBOX`를 더하고 `clear()`.

호출부 갱신 — 각 화면이 실제로 바꾼 묶음을 선언한다:

- `sprint.ts` 두 곳: `putDay(day, ['sprint'])`
- `grade.ts`: `putDay(updated, ['grades'])`
- `print-sheet.ts` 두 곳(자동 생성·다시 만들기): `putDay(day, ['sheet'])`

- [ ] **Step 4: 전체 테스트** — `npx vitest run` → 전부 PASS (기존 303 + 신규)
- [ ] **Step 5: 빌드 확인** — `npm run build` → 통과 (putDay 시그니처 누락 호출부가 있으면
      여기서 잡힌다)
- [ ] **Step 6: 커밋**

```bash
git add src/data/db.ts src/data/db.test.ts src/screens/sprint.ts src/screens/grade.ts src/screens/print-sheet.ts
git commit -m "feat: putDay가 같은 트랜잭션으로 아웃박스 표식을 남긴다"
```

---

### Task 5: `sync.ts` — push 엔진과 서버 RPC 호출

**Files:**

- Create: `src/data/sync-config.ts`
- Create: `src/data/sync.ts`

**Interfaces:**

- Consumes: Task 2 `foldOutbox`, Task 4 `getOutbox`·`deleteOutboxThrough`·`getDay`·`getMeta`·
  `getDeviceState`·`putDeviceState`
- Produces:
  - `syncEnabled(): Promise<boolean>` — config 채워짐 && `deviceKey` 존재
  - `kickPush(): void` — 진행 중이면 무시(단일 비행), 끝나면 아웃박스가 비었는지 재확인
  - `serverSnapshot(reason: 'reset' | 'import', payload: { days: Day[]; meta: Meta }): Promise<{ id: number; at: string; dayCount: number }>`
  - `serverReplaceAll(payload: { days: Day[]; meta: Meta }): Promise<void>`
  - `listSnapshots(limit: number): Promise<{ id: number; at: string; reason: string; dayCount: number }[]>`
  - `getSnapshotPayload(id: number): Promise<{ days: Day[]; meta: Meta }>`
  - `serverOnline(): Promise<boolean>` — `GET /rest/v1/meta?select=id`가 2xx인지 (§6의
    "온라인 확인"이 이걸 쓴다 — `navigator.onLine`은 와이파이만 볼 뿐 서버 일시정지를 모른다)

- [ ] **Step 1: `sync-config.ts` 작성**

```ts
/**
 * Supabase 접속 정보. 사람이 채워 커밋한다(supabase/README.md 4단계) — 둘 다 공개 전제라
 * 커밋해도 된다(RLS가 막는다). 기기 키는 절대 여기 두지 않는다 — device 스토어에만 산다.
 * 비어 있으면 동기화 전체가 조용히 꺼진다(배포 안전: 서버 준비 전에 머지돼도 무해).
 */
export const SUPABASE_URL = ''
export const SUPABASE_ANON_KEY = ''
```

- [ ] **Step 2: `sync.ts` 작성** — 얇게. 로직은 이미 engine에 있고 여기는 조율만

```ts
import { foldOutbox } from '../engine/outbox'
import type { Day, Meta } from './types'
import {
  deleteOutboxThrough,
  getDay,
  getDeviceState,
  getMeta,
  getOutbox,
  putDeviceState,
} from './db'
import { SUPABASE_ANON_KEY, SUPABASE_URL } from './sync-config'

const SCHEMA_VERSION = 1

async function headers(): Promise<Record<string, string>> {
  const device = await getDeviceState()
  return {
    apikey: SUPABASE_ANON_KEY,
    'x-device-key': device.deviceKey ?? '',
    'Content-Type': 'application/json',
  }
}

export async function syncEnabled(): Promise<boolean> {
  if (!SUPABASE_URL || !SUPABASE_ANON_KEY) return false
  return (await getDeviceState()).deviceKey !== null
}

/** GET이 2xx면 서버가 깨어 있고 키가 유효하다. navigator.onLine으로는 알 수 없는 정보다. */
export async function serverOnline(): Promise<boolean> {
  try {
    const res = await fetch(`${SUPABASE_URL}/rest/v1/meta?select=id`, { headers: await headers() })
    return res.ok
  } catch {
    return false
  }
}

let pushing = false

/** 아웃박스를 비운다. 단일 비행 — 도는 중의 재요청은 무시하고, 끝에 잔여분을 재확인한다. */
export function kickPush(): void {
  if (pushing) return
  pushing = true
  void pushOutbox()
    .catch(() => {}) // 실패는 아웃박스에 남는 것으로 표현된다 — 여기서 알리지 않는다(§3 조용한 재시도)
    .finally(() => {
      pushing = false
    })
}

async function pushOutbox(): Promise<void> {
  if (!(await syncEnabled())) return
  const raw = await getOutbox()
  if (raw.length === 0) return
  // push 시작 시점의 최대 key — 이후 생긴 표식은 지우지 않는다(설계 §3, Fable 리뷰 5)
  const maxKey = raw[raw.length - 1]!.key
  const folded = foldOutbox(raw)
  for (const entry of folded) {
    if (entry.target === 'meta') await pushMeta()
    else await pushDay(entry.target.slice('day:'.length), entry.bundleAt)
    await deleteOutboxThrough(entry.target, maxKey)
  }
  const device = await getDeviceState()
  await putDeviceState({ ...device, lastSyncAt: new Date().toISOString() })
}

/** rev 프로토콜(설계 §3): INSERT rev=1, PATCH는 ?rev=eq.N + rev=N+1. upsert 금지. 3회 시도. */
async function pushDay(
  date: string,
  bundleAt: Partial<Record<'sheet' | 'grades' | 'sprint', string>>,
): Promise<void> {
  const day = await getDay(date)
  if (!day) return // 표식만 남고 Day가 지워진 경우(초기화 직후) — 보낼 것이 없다
  const device = await getDeviceState()
  const stamps: Record<string, string> = {}
  if (bundleAt.sheet) stamps['sheet_at'] = bundleAt.sheet
  if (bundleAt.grades) stamps['grades_at'] = bundleAt.grades
  if (bundleAt.sprint) stamps['sprint_at'] = bundleAt.sprint

  for (let attempt = 0; attempt < 3; attempt++) {
    const cur = await fetch(`${SUPABASE_URL}/rest/v1/days?date=eq.${date}&select=rev`, {
      headers: await headers(),
    })
    if (!cur.ok) throw new Error(`days 조회 실패: ${cur.status}`)
    const rows = (await cur.json()) as { rev: number }[]

    if (rows.length === 0) {
      const res = await fetch(`${SUPABASE_URL}/rest/v1/days`, {
        method: 'POST',
        headers: await headers(),
        body: JSON.stringify({
          date,
          payload: day,
          rev: 1,
          schema_version: SCHEMA_VERSION,
          device: device.deviceId,
          ...stamps,
        }),
      })
      if (res.ok) return
      if (res.status !== 409) throw new Error(`days 생성 실패: ${res.status}`)
      continue // PK 충돌 — 다른 기기가 먼저 만듦. 재조회해 PATCH 경로로
    }

    const rev = rows[0]!.rev
    const res = await fetch(`${SUPABASE_URL}/rest/v1/days?date=eq.${date}&rev=eq.${rev}`, {
      method: 'PATCH',
      headers: { ...(await headers()), Prefer: 'return=representation' },
      body: JSON.stringify({
        payload: day,
        rev: rev + 1,
        schema_version: SCHEMA_VERSION,
        device: device.deviceId,
        ...stamps,
      }),
    })
    if (!res.ok) throw new Error(`days 갱신 실패: ${res.status}`)
    const updated = (await res.json()) as unknown[]
    if (updated.length > 0) return // 0행이면 rev 충돌 — 재시도
  }
  throw new Error(`rev 충돌 3회: ${date}`)
}

async function pushMeta(): Promise<void> {
  const meta = await getMeta()
  const device = await getDeviceState()
  for (let attempt = 0; attempt < 3; attempt++) {
    const cur = await fetch(`${SUPABASE_URL}/rest/v1/meta?id=eq.1&select=rev`, {
      headers: await headers(),
    })
    if (!cur.ok) throw new Error(`meta 조회 실패: ${cur.status}`)
    const rows = (await cur.json()) as { rev: number }[]
    const rev = rows[0]?.rev ?? 0 // 행은 스키마가 시딩한다 — 없으면 스키마 미적용
    const res = await fetch(`${SUPABASE_URL}/rest/v1/meta?id=eq.1&rev=eq.${rev}`, {
      method: 'PATCH',
      headers: { ...(await headers()), Prefer: 'return=representation' },
      body: JSON.stringify({ payload: meta, rev: rev + 1, device: device.deviceId }),
    })
    if (!res.ok) throw new Error(`meta 갱신 실패: ${res.status}`)
    if (((await res.json()) as unknown[]).length > 0) return
  }
  throw new Error('meta rev 충돌 3회')
}

export async function serverSnapshot(
  reason: 'reset' | 'import',
  payload: { days: Day[]; meta: Meta },
): Promise<{ id: number; at: string; dayCount: number }> {
  const device = await getDeviceState()
  const res = await fetch(`${SUPABASE_URL}/rest/v1/snapshots`, {
    method: 'POST',
    headers: { ...(await headers()), Prefer: 'return=representation' },
    body: JSON.stringify({
      device: device.deviceId,
      reason,
      day_count: payload.days.length,
      payload,
    }),
  })
  if (!res.ok) throw new Error(`스냅샷 실패: ${res.status}`)
  const [row] = (await res.json()) as { id: number; at: string; day_count: number }[]
  return { id: row!.id, at: row!.at, dayCount: row!.day_count }
}

export async function serverReplaceAll(payload: { days: Day[]; meta: Meta }): Promise<void> {
  const res = await fetch(`${SUPABASE_URL}/rest/v1/rpc/replace_all`, {
    method: 'POST',
    headers: await headers(),
    body: JSON.stringify({ p_payload: payload }),
  })
  if (!res.ok) throw new Error(`replace_all 실패: ${res.status}`)
}

export async function listSnapshots(
  limit: number,
): Promise<{ id: number; at: string; reason: string; dayCount: number }[]> {
  const res = await fetch(
    `${SUPABASE_URL}/rest/v1/snapshots?select=id,at,reason,day_count&order=id.desc&limit=${limit}`,
    { headers: await headers() },
  )
  if (!res.ok) throw new Error(`스냅샷 목록 실패: ${res.status}`)
  const rows = (await res.json()) as { id: number; at: string; reason: string; day_count: number }[]
  return rows.map((r) => ({ id: r.id, at: r.at, reason: r.reason, dayCount: r.day_count }))
}

export async function getSnapshotPayload(id: number): Promise<{ days: Day[]; meta: Meta }> {
  const res = await fetch(`${SUPABASE_URL}/rest/v1/snapshots?id=eq.${id}&select=payload`, {
    headers: await headers(),
  })
  if (!res.ok) throw new Error(`스냅샷 조회 실패: ${res.status}`)
  const rows = (await res.json()) as { payload: { days: Day[]; meta: Meta } }[]
  if (rows.length === 0) throw new Error('스냅샷이 없다')
  return rows[0]!.payload
}
```

- [ ] **Step 3: 빌드 확인** — `npm run build` → 통과. 테스트는 없다(네트워크 조율 —
      설계 §12 선 밖. 순수 부분은 이미 Task 2·3에서 검증됨)
- [ ] **Step 4: 커밋**

```bash
git add src/data/sync-config.ts src/data/sync.ts
git commit -m "feat: 아웃박스 push 엔진과 서버 RPC 호출을 추가한다"
```

---

### Task 6: `main.ts` — push 트리거 배선

**Files:**

- Modify: `src/main.ts`

**Interfaces:**

- Consumes: Task 5 `kickPush`

- [ ] **Step 1: 배선 추가** — `navigator.storage.persist()` 호출 근처에:

```ts
import { kickPush } from './data/sync'

// push 트리거(설계 §3): 시작 시 + 표식 생성 시 + 탭 복귀 시. pull은 2단계다.
// 아이 화면에서도 push는 돈다 — 스프린트 결과가 즉시 올라가는 것이 A-1의 핵심이고,
// push는 화면과 무관하게 배경에서만 돈다(실패해도 아무것도 띄우지 않는다).
kickPush()
window.addEventListener('haruchi:outbox', kickPush)
document.addEventListener('visibilitychange', () => {
  if (document.visibilityState === 'visible') kickPush()
})
```

- [ ] **Step 2: 검사** — `npm run build` && `npx vitest run` → 통과
- [ ] **Step 3: 커밋**

```bash
git add src/main.ts
git commit -m "feat: 동기화 push 트리거를 배선한다"
```

---

### Task 7: `confirmDialog`에 `requireText` 옵션

**Files:**

- Modify: `src/ui.ts` (confirmDialog, `ui.ts:167` 부근)

**Interfaces:**

- Produces: `confirmDialog(opts: { …기존…; requireText?: string })` — 값이 있으면 다이얼로그에
  입력란이 생기고, **입력값이 `requireText`와 정확히 일치할 때까지 확인 버튼이 disabled**

- [ ] **Step 1: 구현** — description 렌더 뒤, footer 앞에 삽입:

```ts
// 오조작 방지 입력(설계 §6 3단계). 버튼 한 번은 실수로 눌리지만 글자를 실수로 칠 수는 없다.
// input.value 는 어디에도 렌더되지 않는다 — XSS 경계(innerHTML 금지)는 그대로다.
let gate: HTMLInputElement | null = null
if (opts.requireText) {
  const hint = document.createElement('p')
  hint.className = 'seed-dialog__description'
  hint.textContent = `계속하려면 "${opts.requireText}"를 입력하세요`
  gate = document.createElement('input')
  gate.className = 'confirm-gate seed-text-field__input'
  gate.setAttribute('inputmode', 'text')
  gate.setAttribute('autocomplete', 'off')
  header.append(hint, gate)
}
// confirm 버튼 생성 직후:
if (gate) {
  confirm.disabled = true
  gate.addEventListener('input', () => {
    confirm.disabled = gate!.value !== opts.requireText
  })
}
```

`.confirm-gate`가 SEED text-field 레시피 클래스만으로 부족해 보이면 `app.css`에 **개별 속성**
으로 최소 스타일(폭 100%·패딩)만 더한다 — shorthand(`font` 등) 금지(CSS 레이어 함정).

- [ ] **Step 2: 검사** — `npm run build` && `npx vitest run` → 통과 (DOM 테스트는 만들지
      않는다 — 설계 §12)
- [ ] **Step 3: 커밋**

```bash
git add src/ui.ts src/styles/app.css
git commit -m "feat: confirmDialog에 타이핑 확인 옵션을 더한다"
```

---

### Task 8: 부모 홈 — 미등록 안내·키 등록·상태줄

**Files:**

- Modify: `src/screens/home-parent.ts`

**Interfaces:**

- Consumes: Task 3 `syncStatus`, Task 4 `getDeviceState`·`putDeviceState`·`getOutbox`,
  Task 5 `kickPush`
- Produces: 없음 (말단 화면)

- [ ] **Step 1: 렌더 데이터 수집** — `renderParentHome`의 기존 `getAllDays`/`getMeta` 옆에:

```ts
const device = await getDeviceState()
const outbox = await getOutbox()
const status = syncStatus({
  registered: device.deviceKey !== null,
  outboxCount: outbox.length,
  lastSyncAt: device.lastSyncAt,
  today,
})
```

- [ ] **Step 2: 상태줄·등록 폼 렌더** — 기존 `.links` 위에 삽입. `tone: 'setup'`이면 진행
      상태 대신 등록 블록을 그린다:

```ts
const syncHtml =
  status.tone === 'setup'
    ? `<div class="sync-setup">
        <p>${escapeHtml(status.lines[0]!)}</p>
        <p class="sync-device-id">기기 id: <code>${escapeHtml(device.deviceId)}</code></p>
        <input id="device-key" type="password" autocomplete="off" placeholder="기기 키 붙여넣기" />
        <button id="device-key-save" class="step">연결하기</button>
        <p class="sync-hint">키 발급 방법은 supabase/README.md</p>
      </div>`
    : `<div class="sync-status ${status.tone === 'warn' ? 'sync-warn' : ''}">
        ${status.lines.map((l) => `<div>${escapeHtml(l)}</div>`).join('')}
      </div>`
```

핸들러 — 저장 후 즉시 push를 차서 등록이 실제로 통하는지 아빠가 바로 본다:

```ts
root.querySelector('#device-key-save')?.addEventListener('click', () => {
  const input = root.querySelector<HTMLInputElement>('#device-key')!
  const key = input.value.trim()
  if (!key) return
  void putDeviceState({ ...device, deviceKey: key }).then(() => {
    kickPush()
    navigate('#/parent') // 같은 해시 재라우팅은 안전하다(상태를 IndexedDB에서 다시 읽는다)
  })
})
```

`sync-config.ts`가 비어 있으면(`SUPABASE_URL === ''`) 등록 블록 대신 아무것도 그리지 않는다 —
서버 준비 전 배포에서 부모 홈이 지금과 완전히 같게 보이는 조건이다.

- [ ] **Step 3: 스타일 최소 추가** — `app.css`에 `.sync-status`(작은 회색 글자)·`.sync-warn`
      (경고 톤 — `var(--seed-color-fg-critical)` 같은 토큰 참조, 값 복사 금지)·`.sync-setup`
- [ ] **Step 4: 검사** — `npm run build` && `npx vitest run` && `npm run dev`로 육안 확인
      (미등록 상태 블록이 뜨는지, config 비면 아무것도 안 뜨는지)
- [ ] **Step 5: 커밋**

```bash
git add src/screens/home-parent.ts src/styles/app.css
git commit -m "feat: 부모 홈에 기기 등록과 동기화 상태줄을 더한다"
```

---

### Task 9: `report.ts` — §6 파괴 경로 재작성 + 스냅샷 되돌리기

**Files:**

- Modify: `src/screens/report.ts` (초기화 `report.ts:400-449` 부근, 가져오기 `report.ts:340-395`
  부근 — PR #18 이후 기준)

**Interfaces:**

- Consumes: Task 5 `syncEnabled`·`serverOnline`·`serverSnapshot`·`serverReplaceAll`·
  `listSnapshots`·`getSnapshotPayload`, Task 7 `requireText`
- Produces: 없음 (말단 화면)

- [ ] **Step 1: 초기화 흐름 재작성** — 기존 `confirmDialog` 호출을 감싸는 순서(설계 §6 실행
      순서 — **순서 자체가 안전장치다**):

```ts
// 동기화가 켜져 있으면: 온라인 확인 → 사전 스냅샷 → 타이핑 확인 → 로컬 → 서버.
// 꺼져 있으면(미등록·config 비움): 기존 로컬 흐름 그대로 — 회귀 없음.
const enabled = await syncEnabled()
let snapshotLine: string[] = []
if (enabled) {
  if (!(await serverOnline())) {
    setResetBusy(false)
    showError('서버에 연결할 수 없어요. 백업을 만들 수 없는 동안은 지울 수 없어요.')
    return
  }
  const snap = await serverSnapshot('reset', { days, meta }) // 실패하면 catch로 — 아무것도 안 지운다
  snapshotLine = [`방금 서버에 ${dayCount(snap.dayCount)} 백업을 만들었어요`]
}
void confirmDialog({
  title: '모든 기록을 지울까요?',
  description: [
    `${dayCount(days.length)} 기록(${range})이 사라지고 처음 상태로 돌아가요.`,
    ...(ungraded > 0
      ? [
          `⚠ 아직 채점하지 않은 문제지가 ${dayCount(ungraded)} 있어요 — 그 종이는 채점할 수 없게 돼요`,
        ]
      : []),
    backupLine,
    ...snapshotLine,
  ],
  confirmLabel: '네, 지울게요',
  cancelLabel: '취소',
  tone: 'critical',
  requireText: enabled ? '지우기' : undefined, // 동기화 전에는 기존 UX 유지
}).then(async (ok) => {
  if (!ok) {
    setResetBusy(false)
    return
  }
  await resetAll() // 4. 로컬 먼저 — 서버 삭제 실패는 "도로 내려옴"으로 드러나고 데이터를 잃지 않는다
  if (enabled) await serverReplaceAll({ days: [], meta: defaultMeta() }) // 5. RPC가 자동 스냅샷 포함
  // 이하 기존 navigate('#/parent') 흐름 유지, catch는 기존 showError 패턴
})
```

- [ ] **Step 2: 가져오기 흐름에 같은 감싸기** — `replaceAll(v.days, v.meta)` 직전에
      `serverSnapshot('import', …)`(동기화 켜짐일 때), 성공 후 `serverReplaceAll(v)` 호출.
      `requireText: '지우기'`도 동일하게. 기존 `importBusy` 가드 **안쪽**에 넣는다(설계 §6 —
      스냅샷 업로드 중 다른 파괴 경로가 열리면 PR #18이 정리한 경합이 되살아난다)
- [ ] **Step 3: 스냅샷 되돌리기 블록** — `#/report` 하단, 초기화 버튼 아래(동기화 켜짐 +
      온라인일 때만 렌더):

```ts
// 최근 5개. 서버 유래 값(at·reason)은 escapeHtml을 거친다 — 백업 파일과 같은 신뢰 등급.
const snaps = await listSnapshots(5).catch(() => [])
const REASON_LABELS: Record<string, string> = {
  reset: '초기화 전',
  import: '가져오기 전',
  auto: '자동',
  'generation-conflict': '동기화 충돌',
}
// 렌더: 줄마다 "MM/DD HH:mm · <라벨> · N일치" + [이걸로 되돌리기] 버튼
// 클릭: confirmDialog({ title: '이 시점으로 되돌릴까요?', requireText: '되돌리기', … })
//   → getSnapshotPayload(id) → validateBackup 형식 검사(스냅샷도 신뢰하지 않는다)
//   → replaceAll(로컬) → serverReplaceAll(같은 payload) → toast('되돌렸어요') → 재라우팅
```

되돌리기도 `importBusy` 가드를 같이 쓴다 — 복구·초기화·가져오기 셋이 서로를 막는 기존 구조
그대로.

- [ ] **Step 4: 검사** — `npm run build` && `npx vitest run` && dev 서버 육안: config 비운
      상태에서 초기화·가져오기가 **지금과 완전히 동일**하게 동작하는지(회귀 확인이 핵심)
- [ ] **Step 5: 커밋**

```bash
git add src/screens/report.ts
git commit -m "feat: 초기화·가져오기가 서버 스냅샷 뒤에만 진행된다"
```

---

### Task 10: 주 1회 ping 워크플로

**Files:**

- Create: `.github/workflows/ping-supabase.yml`

- [ ] **Step 1: 작성**

```yaml
# Supabase 무료 티어는 ~1주 무요청이면 일시정지된다(설계 「감수한 것」). 주 1회 깨운다.
# anon 키는 공개 전제(RLS가 막는다) — 시크릿이 아니라 평문으로 둔다. 사람이 값을 채운다.
name: ping-supabase
on:
  schedule:
    - cron: '0 21 * * 0' # 일요일 21:00 UTC = 월요일 아침 KST
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: ping
        run: |
          # TODO(사람): URL과 anon 키를 채운다 — supabase/README.md 5단계
          curl --fail --silent --show-error \
            -H "apikey: <SUPABASE_ANON_KEY>" \
            "<SUPABASE_URL>/rest/v1/meta?select=id" > /dev/null
```

플레이스홀더가 남은 동안은 워크플로가 실패한다 — **조용히 성공하는 척하는 것보다 낫다**
(Actions 실패 메일이 "아직 안 채웠다"를 알린다).

- [ ] **Step 2: 검사 후 커밋**

```bash
npx prettier --check .
git add .github/workflows/ping-supabase.yml
git commit -m "feat: Supabase 일시정지 예방 주간 ping을 추가한다"
```

---

### Task 11: 문서 갱신과 마감

**Files:**

- Modify: `CLAUDE.md` (첫 줄 단서·아키텍처 절)
- Modify: `docs/superpowers/HANDOFF.md`

- [ ] **Step 1: CLAUDE.md 갱신** — "단, 동기화 백엔드가 설계 승인돼 있다" 단락을 1단계 현실로
      교체:

```markdown
**동기화 1단계(업로드)가 배선돼 있다**(2026-08-06 설계, `docs/superpowers/specs/2026-08-06-sync-backend-design.md`).
IndexedDB가 여전히 원본이고 Supabase는 단방향 복제본이다 — pull·병합은 2단계다.
`putDay(day, changed)`는 바꾼 묶음을 선언해야 하고(`sheet`|`grades`|`sprint`), 같은
트랜잭션으로 아웃박스 표식이 남는다. `sync-config.ts`가 비어 있으면 동기화 전체가 꺼진다.
파괴적 경로(초기화·가져오기)는 동기화가 켜져 있으면 서버 스냅샷 뒤에만 진행된다.
```

아키텍처 절의 디렉터리 목록에 `src/data/sync.ts` 한 줄, 불변식 절에 "putDay의 changed 선언"
한 줄을 더한다.

- [ ] **Step 2: HANDOFF.md 갱신** — 새 절 「동기화 1단계」: 무엇이 들어갔고, 2단계로 넘긴 것
      (pull·merge.ts·PIN·등록 완성형·백업 경고 기준 전환·인쇄 생성 게이트), 사람 확인 대기
      목록(아래 Step 4)
- [ ] **Step 3: 전체 검사**

```bash
npm run format && npx prettier --check . && npx vitest run && npm run build
```

- [ ] **Step 4: 커밋 후 PR** — squash 전용 레포. PR 제목이 곧 main 커밋 메시지다

```bash
git add CLAUDE.md docs/superpowers/HANDOFF.md
git commit -m "docs: 동기화 1단계를 CLAUDE.md와 HANDOFF에 반영한다"
git push -u origin sync-phase1
gh pr create --title "feat: 동기화 1단계 — 기록이 Supabase에 단방향으로 쌓인다" --body "..."
```

PR 본문에 **사람 확인 체크리스트**를 명시한다(머지 전 supabase/README.md 절차 완료 필요):

1. Supabase 프로젝트 생성·스키마 적용·아이패드 등록(README 1~6)
2. 문제지 인쇄 → `days`에 행 생성, `sheet_at`만 찍힘
3. 스프린트 → 같은 행 갱신, `sprint_at`만 갱신되고 `sheet_at` 불변 (아웃박스 `bundles` 검증)
4. 채점 저장 → `grades_at` 갱신
5. 비행기 모드에서 스프린트 → 부모 홈 "안 올라간 기록 N건" → 와이파이 복귀 → 올라가고 경고 소멸
6. 초기화: 스냅샷 없이는 진행 안 됨·"지우기" 타이핑·서버 `days` 비워짐·`snapshots` 2건
   (사전 + auto)·되돌리기로 복구
7. 키를 `revoked_at`으로 폐기 → 즉시 401 → 부모 홈 인증 실패 표시
8. `sync-config.ts`를 비운 빌드에서 앱이 지금과 완전히 동일하게 동작

---

## 자체 검토 기록

- **스펙 커버리지**: 1단계 범위(설계 「배포 단계」) 전체 매핑 — 아웃박스 push(Task 2·4·5·6),
  스키마 전체(Task 1), 키 입력 최소형(Task 8), §6 전부(Task 9 + RPC는 Task 1·5), 부모 홈
  경고(Task 3·8), Actions ping(Task 10). 2단계 항목(pull·merge·PIN·생성 게이트·백업 경고
  전환)은 의도적으로 없음
- **타입 일관성**: `SyncBundle`·`OutboxEntry`(Task 2) ← db.ts(Task 4) ← sync.ts(Task 5).
  `putDay(day, changed)` 시그니처는 Task 4에서 바꾸고 같은 태스크에서 호출부 4곳을 갱신 —
  빌드가 누락을 잡는다
- **플레이스홀더**: ping 워크플로의 `<SUPABASE_URL>` 등은 의도된 사람-채움 표식이고 문서에
  그렇게 명시돼 있다. Task 9 Step 3의 렌더 상세는 주석 골격이지만 데이터·확인 흐름·이스케이프
  규칙이 전부 명시돼 있어 구현 재량 범위다
