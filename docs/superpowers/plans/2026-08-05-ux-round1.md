# UX 리뷰 1라운드 반영 — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `docs/design/ux-review-2026-08-05.md`의 발견 전체(P1 4·P2 8·P3·카피)를 사용자 결정 4건에 따라 반영한다.

**Architecture:** 유일한 엔진 작업은 mood 기반 세로셈 문항 수 파생(`deriveVerticalCount`) 하나이고, 나머지는 전부 화면 템플릿·CSS·문구 수정이다. 파생은 이 레포의 "로그는 사실, 파생은 해석" 원칙대로 저장 없이 매번 재계산한다 — 스키마 변경·마이그레이션이 없고 이미 쌓인 mood가 소급 적용된다.

**Tech Stack:** 바닐라 DOM + TypeScript, vitest, SEED CSS 토큰(설치본에서 존재 확인 완료: `--seed-color-fg-critical` · `--seed-color-bg-disabled` · `--seed-color-fg-disabled` · `--seed-color-stroke-positive-solid`).

**사용자 결정 (2026-08-05):** ① mood는 설계 §6.8대로 배선한다 ② 스프린트에 조용한 출구를 추가한다 ③ 운영자 호칭은 **"부모"**로 통일한다(brand.md 권장과 반대 — 문서도 이 결정에 맞춰 갱신) ④ 범위는 전체 일괄.

## Global Constraints

- 모든 npm 명령 전: `export PATH="$HOME/.local/share/mise/installs/node/lts/bin:$PATH"`
- 작업 브랜치 `ux-round1`에서 진행한다. 브랜치 push는 배포를 유발하지 않는다. 머지는 PR squash 전용(CLAUDE.md 「머지와 충돌」)
- `git add`는 항상 명시 경로. `git add .` 금지
- docs를 커밋하기 전 반드시 `npm run format`
- **재인쇄 불변식을 건드리지 않는다**: `print-sheet.ts`의 `if (!day || day.sheet.length === 0)` 게이트와 `print.css`의 종이 클래스(`.sheet*`·`.v*`·`.inv*`·`.strat*`·`.word*`·`.n`)는 이 계획의 어떤 태스크도 수정하지 않는다
- **아이 소속 화면(`#/`·`#/sprint`·`#/map`·`#/ebs`)에서 부모 소속 화면으로 가는 `navigate()`를 만들지 않는다** (새 navigate는 Task 3의 `#/` 하나뿐)
- 템플릿에 이스케이프 없이 들어가는 값은 우리가 만든 리터럴뿐 — 이 계획의 새 보간은 `formatDate(정규식 검증된 날짜)` 하나이고 안전하다
- CSS 리셋에 `font`·`background`·`all` shorthand 금지 (CLAUDE.md CSS 레이어 전략)
- 화면(DOM) 테스트는 만들지 않는다(설계 §12). 화면 태스크는 "사람 확인" 항목을 남기는 것으로 갈음한다
- 테스트를 추가하는 태스크는 **변이 검증**(구현을 일부러 틀리게 → 해당 테스트만 빨간지 확인 → 원복)을 스텝으로 포함한다

---

### Task 0: 브랜치 생성

**Files:** 없음 (git만)

- [ ] **Step 1:** `git switch -c ux-round1`

---

### Task 1: 엔진 — `deriveVerticalCount` (mood 배선의 심장)

**Files:**

- Modify: `src/engine/derive.ts` (파일 끝에 함수 추가)
- Test: `src/engine/derive.test.ts` (describe 블록 추가)

**Interfaces:**

- Consumes: `Day.mood?: 'easy' | 'ok' | 'hard'` (types.ts 기존 필드), days 날짜 오름차순 계약(getAllDays)
- Produces: `deriveVerticalCount(days: Day[]): 8 | 6` — Task 2가 print-sheet와 home-parent에서 import한다

**의미 결정(스펙 §6.8이 특정하지 않은 부분, 이 계획의 재량):** "3일 연속"은 달력이 아니라 **기록의 연속**이다. mood가 없는 날은 신호의 부재이지 반증이 아니므로 연속을 끊지 않고, 😐(ok)는 끊는다. 되돌리려면 필터를 달력 검사로 바꾸면 된다 — 파생이라 과거 전체가 새 해석을 따라온다.

- [ ] **Step 1: 실패하는 테스트 작성** — `src/engine/derive.test.ts`에 추가:

```ts
import { deriveVerticalCount } from './derive'
import type { Day, Mood } from '../data/types'

describe('deriveVerticalCount — mood 기반 분량 파생(설계 §6.8 ②)', () => {
  const d = (date: string, mood?: Mood): Day => ({
    date,
    kind: 'normal',
    sheet: [],
    ...(mood ? { mood } : {}),
  })
  const seq = (...moods: (Mood | undefined)[]): Day[] =>
    moods.map((m, i) => d(`2026-08-${String(i + 1).padStart(2, '0')}`, m))

  test('mood 기록이 없으면 8', () => {
    expect(deriveVerticalCount([])).toBe(8)
    expect(deriveVerticalCount(seq(undefined, undefined))).toBe(8)
  })

  test('😫 3연속이면 6으로 내린다', () => {
    expect(deriveVerticalCount(seq('hard', 'hard', 'hard'))).toBe(6)
  })

  test('😫 2연속은 내리지 않는다', () => {
    expect(deriveVerticalCount(seq('hard', 'hard'))).toBe(8)
  })

  test('😐가 끼면 연속이 끊긴다', () => {
    expect(deriveVerticalCount(seq('hard', 'hard', 'ok', 'hard'))).toBe(8)
  })

  test('mood 없는 날은 연속을 끊지 않는다(기록 연속 해석)', () => {
    expect(deriveVerticalCount(seq('hard', undefined, 'hard', undefined, 'hard'))).toBe(6)
  })

  test('하향 후 😀 5연속이면 8로 복구한다', () => {
    expect(
      deriveVerticalCount(seq('hard', 'hard', 'hard', 'easy', 'easy', 'easy', 'easy', 'easy')),
    ).toBe(8)
  })

  test('하향 후 😀 4연속 + 😐면 6 유지', () => {
    expect(
      deriveVerticalCount(seq('hard', 'hard', 'hard', 'easy', 'easy', 'easy', 'easy', 'ok')),
    ).toBe(6)
  })

  test('복구 후 다시 😫 3연속이면 다시 6', () => {
    expect(
      deriveVerticalCount(
        seq('hard', 'hard', 'hard', 'easy', 'easy', 'easy', 'easy', 'easy', 'hard', 'hard', 'hard'),
      ),
    ).toBe(6)
  })
})
```

- [ ] **Step 2: 실패 확인** — `npx vitest run src/engine/derive.test.ts` → `deriveVerticalCount` export 없음으로 FAIL
- [ ] **Step 3: 구현** — `src/engine/derive.ts` 끝에:

```ts
/**
 * mood 로그에서 오늘 문제지의 세로셈 문항 수를 파생한다(설계 §6.8 ②).
 * 😫(hard) 3연속 → 6으로 하향, 하향 상태에서 😀(easy) 5연속 → 8로 복구.
 *
 * "연속"은 달력이 아니라 기록의 연속이다 — mood가 없는 날(채점을 안 했거나
 * 기분을 안 고른 날)은 신호의 부재이지 반증이 아니라 연속을 끊지 않는다.
 * 😐(ok)는 끊는다. 재량 결정(2026-08-05, plans/2026-08-05-ux-round1.md).
 *
 * 다른 derive와 같은 원칙 — 저장하지 않고 매번 재계산한다. 규칙(3일/5일)을
 * 바꾸면 과거 mood 전체가 새 규칙으로 소급 재해석된다. Settings.verticalCount는
 * 기본값(8)의 자리로만 남는다 — 이 함수의 결과가 화면·조립의 실효값이다.
 */
export function deriveVerticalCount(days: Day[]): 8 | 6 {
  let count: 8 | 6 = 8
  let run = 0
  for (const day of days) {
    if (!day.mood) continue
    if (count === 8) {
      run = day.mood === 'hard' ? run + 1 : 0
      if (run >= 3) {
        count = 6
        run = 0
      }
    } else {
      run = day.mood === 'easy' ? run + 1 : 0
      if (run >= 5) {
        count = 8
        run = 0
      }
    }
  }
  return count
}
```

- [ ] **Step 4: 통과 확인** — `npx vitest run src/engine/derive.test.ts` → 전부 PASS
- [ ] **Step 5: 변이 검증** — ① `run >= 3`을 `run >= 2`로 바꾸고 실행 → "😐가 끼면 연속이 끊긴다"와 "😫 2연속은 내리지 않는다"만 FAIL 확인 → 원복. ② `run >= 5`를 `run >= 4`로 → "😀 4연속 + 😐면 6 유지"만 FAIL 확인 → 원복. ③ `if (!day.mood) continue`를 `if (!day.mood) { run = 0; continue }`로 → "mood 없는 날은 연속을 끊지 않는다"만 FAIL 확인 → 원복
- [ ] **Step 6: 전체 테스트** — `npm test` → 기존 295 + 신규 8 = 303 PASS
- [ ] **Step 7: 커밋** — `git add src/engine/derive.ts src/engine/derive.test.ts && git commit -m "feat: mood 로그에서 세로셈 문항 수를 파생한다 — 설계 §6.8 ② 배선의 엔진"`

---

### Task 2: mood 배선 — 화면 두 곳이 파생값을 쓰게

**Files:**

- Modify: `src/screens/print-sheet.ts` (buildSheet)
- Modify: `src/screens/home-parent.ts` (인쇄 버튼 sub 라벨)
- Modify: `src/screens/grade.ts` (mood 질문에 용도 한 줄 — 배선됐음을 광고)

**Interfaces:**

- Consumes: `deriveVerticalCount(days): 8 | 6` (Task 1)

**중요:** 문제지를 실제로 만드는 곳(print-sheet)과 문항 수를 표시하는 곳(home-parent)이 **같은 파생값**을 써야 한다 — 어긋나면 화면이 거짓말한다(원칙 3·5). 이미 만들어진 sheet는 게이트 뒤에 있으므로 파생값이 바뀌어도 불변 — 재인쇄 불변식 무영향.

- [ ] **Step 1:** `print-sheet.ts` — import에 `deriveVerticalCount` 추가(`../engine/derive`의 기존 import 줄에), `buildSheet()`의 `composeSheet` 호출을:

```ts
return composeSheet({
  // 세로셈 문항 수는 설정이 아니라 mood 로그의 파생이다(설계 §6.8 ②,
  // derive.ts의 deriveVerticalCount 주석 참고). home-parent.ts의 문항 수
  // 라벨도 같은 파생을 쓴다 — 한쪽만 바꾸면 화면이 거짓말한다.
  settings: { ...meta.settings, verticalCount: deriveVerticalCount(days) },
  types,
  strategies,
  facts,
  lastSeen: deriveLastSeen(days),
  today: dayKey(new Date()),
})
```

- [ ] **Step 2:** `home-parent.ts` — import에 `deriveVerticalCount` 추가(`../engine/report` 옆에 `../engine/derive`), 렌더 앞에 `const verticalCount = deriveVerticalCount(days)`를 두고 sub 라벨의 `meta.settings.verticalCount` 두 곳을 `verticalCount`로 교체:

```ts
<small>세로셈 ${verticalCount} + □ 채우기 ${meta.settings.inverseCount} + 생각하는 문제 ${THINKING_ITEMS_PER_DAY} (${verticalCount + meta.settings.inverseCount + THINKING_ITEMS_PER_DAY}문항 · 2장)</small>
```

- [ ] **Step 3:** `grade.ts` — mood 질문 줄을 교체(배선 사실을 알린다 — 지금까지는 어디에도 안 쓰였다):

```ts
<div class="date" style="margin-top:20px">오늘 어땠어? (내일 분량에 반영돼요)</div>
```

- [ ] **Step 4:** `npm test && npm run build` → PASS
- [ ] **Step 5: 커밋** — `git add src/screens/print-sheet.ts src/screens/home-parent.ts src/screens/grade.ts && git commit -m "feat: mood 파생을 문제지 조립과 부모 홈 라벨에 배선한다"`

**사람 확인(머지 전 브라우저):** 채점에서 😫를 3일 연속 저장(날짜 조작 필요 — dev에서 IndexedDB에 mood 있는 Day 3개 주입)하면 부모 홈 라벨과 새 문제지가 세로셈 6이 되는지.

---

### Task 3: 스프린트 조용한 출구

**Files:**

- Modify: `src/screens/sprint.ts` (runSession 템플릿 + 리스너)
- Modify: `src/styles/app.css` (`.sprint-top` · `.sprint-exit`)
- Modify: `docs/superpowers/specs/2026-08-02-haruchi-design.md` §7 「스프린트」 한 줄

- [ ] **Step 1:** `sprint.ts` — `runSession`의 템플릿에서 `<div class="sprint-progress" id="bar"></div>` 바로 위에 추가:

```html
<div class="sprint-top"><button class="sprint-exit" id="exit" aria-label="그만하기">✕</button></div>
```

리스너 등록부(`#pad` 리스너 근처)에 추가:

```ts
// 조용한 출구(리뷰 P1-2). 확인창 없음 — 잃는 것은 저장 안 된 세션뿐이고
// 그것은 "중간에 나가면 없던 일"(위 finish 주석) 정책이 의도한 결과다.
// navigate가 hashchange를 쏘고 onHashChange가 cancelled를 세워 이후의
// 예약된 콜백(reveal 타임아웃 등)을 무력화한다 — 새 취소 경로가 아니다.
root.querySelector('#exit')!.addEventListener('click', () => navigate('#/'))
```

- [ ] **Step 2:** `app.css` — `.sprint-progress` 규칙 위에 추가:

```css
/* 스프린트의 조용한 출구(리뷰 P1-2, 2026-08-05 사용자 결정). 키패드 반대편(위)에
 * 두고 크기를 키우지 않는다 — 오누름으로 세션을 잃는 것을 배치로 막되, 터치
 * 최소(44px)는 지킨다. */
.sprint-top {
  display: flex;
  justify-content: flex-end;
}
.sprint-exit {
  width: 44px;
  height: 44px;
  border: none;
  background: none;
  color: var(--muted);
  font-size: var(--seed-font-size-t5);
  cursor: pointer;
}
```

- [ ] **Step 3:** 설계 문서 §7 「스프린트」의 "진행바 · 큰 수식 · 숫자 키패드(`←` `0` `✓`). 타이머 비표시." 문장을 다음으로 교체:

```
진행바 · 큰 수식 · 숫자 키패드(`←` `0` `✓`). 타이머 비표시. 우상단에 조용한
나가기(✕) — 누르면 저장 없이 홈으로 가고 그 세션은 없던 일이 된다(§6.1의
"세션은 끝났을 때만 저장" 정책 그대로. 2026-08-05 사용자 결정, 리뷰 P1-2).
```

- [ ] **Step 4:** `npm run format && npm test && npm run build` → PASS
- [ ] **Step 5: 커밋** — `git add src/screens/sprint.ts src/styles/app.css docs/superpowers/specs/2026-08-02-haruchi-design.md && git commit -m "feat: 스프린트에 조용한 출구를 단다 — 세션 미저장 정책은 그대로"`

**사람 확인:** ✕ 탭 → 즉시 아이 홈, 다시 스프린트 진입 시 새 세션(이전 진행 소실)인지. 오답 reveal 도중 ✕ → 홈 화면이 이후에 덮이지 않는지.

---

### Task 4: 완료/비활성 시각 어휘 분리

**Files:**

- Modify: `src/screens/home-parent.ts` (채점 버튼 disabled)
- Modify: `src/styles/app.css` (`.step:disabled` · `.step.done`)

- [ ] **Step 1:** `home-parent.ts` — 채점 버튼에 disabled 속성(클릭 가드는 방어선으로 유지):

```ts
<button class="step ${graded ? 'done' : ''}" id="grade" ${printed ? '' : 'disabled'}>
```

- [ ] **Step 2:** `app.css` — `.step.done` 블록을 다음으로 교체하고 `.step:disabled`를 추가:

```css
/* 완료는 "됐음"(positive 테두리 + ✓), 비활성은 "지금은 못 누름"(disabled 토큰).
 * 두 상태가 같은 muted 얼굴이던 것을 분리했다(리뷰 P1-3·P2-3). 완료 버튼은
 * 여전히 눌린다 — 인쇄 완료 뒤에도 재인쇄·미리보기가 이 버튼 뒤에 있다. */
.step.done {
  border-color: var(--seed-color-stroke-positive-solid);
}
.step:disabled {
  background: var(--seed-color-bg-disabled);
  color: var(--seed-color-fg-disabled);
  cursor: default;
}
/* 안내문("문제지를 먼저 인쇄해주세요")은 읽혀야 한다 — fg-disabled까지 낮추지 않는다. */
.step:disabled small {
  color: var(--muted);
}
```

- [ ] **Step 3:** `npm test && npm run build` → PASS
- [ ] **Step 4: 커밋** — `git add src/screens/home-parent.ts src/styles/app.css && git commit -m "fix: 완료와 비활성의 시각 어휘를 분리한다 — done은 positive, 못 누르면 disabled"`

**사람 확인:** 인쇄 전 채점 버튼이 눌리지 않는 얼굴인지 / 인쇄 후 인쇄 버튼이 "완료됐지만 눌리는" 얼굴인지.

---

### Task 5: 에러 문구 체계 — 사람 말 + 상세 축소

**Files:**

- Modify: `src/ui.ts` (showError에 detail 인자)
- Modify: `src/styles/app.css` (`.error-content` · `.error-detail`)
- Modify: 호출부 전부 — `src/main.ts` · `src/screens/home-child.ts` · `home-parent.ts` · `sprint.ts` · `map.ts` · `ebs.ts` · `grade.ts` · `report.ts` · `print-sheet.ts`

**Interfaces:**

- Produces: `showError(message: string, detail?: unknown): void` — message는 사람 말, detail은 작게 표시되는 기술 상세(Error면 .message만)

- [ ] **Step 1:** `ui.ts` — showError를 다음으로 교체(생성부와 갱신부 모두):

```ts
export function showError(message: string, detail?: unknown): void {
  let bar = document.querySelector<HTMLDivElement>('#error-bar')
  if (!bar) {
    bar = document.createElement('div')
    bar.id = 'error-bar'
    // .overlay는 인쇄에서 숨겨지는 유일한 표식이다(print.css의 @media print).
    bar.className = 'overlay seed-callout__root seed-callout__root--tone_critical'
    bar.setAttribute('role', 'alert')

    // 사람 말(위)과 기술 상세(아래, 작게)를 나눈다(리뷰 P1-4) — 상세는 아빠의
    // 디버깅용이지 사용자의 읽을거리가 아니다. 둘 다 textContent — XSS 경계.
    const content = document.createElement('div')
    content.className = 'error-content'
    const text = document.createElement('span')
    text.className = 'error-text seed-callout__description'
    const detailLine = document.createElement('small')
    detailLine.className = 'error-detail'
    content.append(text, detailLine)
    bar.append(content)

    const dismiss = document.createElement('button')
    dismiss.className = 'error-dismiss seed-callout__closeButton'
    dismiss.textContent = '✕'
    dismiss.setAttribute('aria-label', '오류 알림 닫기')
    dismiss.addEventListener('click', clearError)
    bar.append(dismiss)

    document.body.prepend(bar)
  }
  bar.querySelector('.error-text')!.textContent = message
  bar.querySelector('.error-detail')!.textContent =
    detail == null ? '' : detail instanceof Error ? detail.message : String(detail)
}
```

- [ ] **Step 2:** `app.css` — `.error-text { flex: 1 }` 블록을 교체:

```css
/* 사람 말 + 기술 상세의 2줄 구조(ui.ts showError). content가 ✕ 옆 남는 폭을
 * 차지한다 — callout root가 display: flex라 flex: 1 하나로 충분하다. */
.error-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 2px;
}
.error-detail {
  color: var(--muted);
  font-size: var(--seed-font-size-t2);
}
```

- [ ] **Step 3:** 호출부 교체 — 패턴은 전부 `showError(\`…: ${(e as Error).message}\`)`→`showError('…', e)`. 파일별 새 문구(어미 "~하지 못했어요" 통일, 리뷰 P3-3):

| 파일             | 새 message (detail은 전부 `e`)                                                                           |
| ---------------- | -------------------------------------------------------------------------------------------------------- |
| `main.ts`        | `'화면을 열지 못했어요. 다시 시도해 주세요.'`                                                            |
| `home-child.ts`  | `'화면을 열지 못했어요.'` — 그리고 본문 `<p class="date">`를 `기록을 열지 못했어요.`로                   |
| `home-parent.ts` | `'화면을 열지 못했어요.'`                                                                                |
| `sprint.ts`      | 열기: `'스프린트를 열지 못했어요.'` / 저장 2곳: `'스프린트 결과를 저장하지 못했어요. 다시 눌러 주세요.'` |
| `map.ts`         | `'지도를 열지 못했어요.'`                                                                                |
| `ebs.ts`         | `'강의 목록을 열지 못했어요.'`                                                                           |
| `grade.ts`       | 열기: `'채점 화면을 열지 못했어요.'` / 저장: `'채점을 저장하지 못했어요. 다시 눌러 주세요.'`             |
| `print-sheet.ts` | 열기·생성: `'문제지를 만들지 못했어요.'` / 재생성: `'문제지를 다시 만들지 못했어요.'`                    |
| `report.ts`      | 기존 문구 유지하되 `: ${...}` 꼬리만 detail 인자로 이동 (가져오기 계열 문구는 이미 사람 말이라 그대로)   |

`report.ts`의 JSON·검증 실패 문구(`'JSON 파일이 아니에요…'`, `` `백업 파일이 아니에요: ${v.reason}` ``)는 v.reason이 우리가 만든 한국어 문장이므로 현행 유지.

- [ ] **Step 4:** `npm test && npm run build` → PASS (tsc가 남은 옛 패턴의 인자 오류를 잡아 준다 — grep으로도 확인: `grep -rn '(e as Error).message' src/screens src/main.ts`에서 showError 인자 위치에 남은 것이 없어야 한다. retrySave 내부 등 message를 직접 쓰는 곳도 같은 패턴으로 바꾼다)
- [ ] **Step 5: 커밋** — `git add src/ui.ts src/styles/app.css src/main.ts src/screens/*.ts && git commit -m "fix: 에러 배너를 사람 말 + 작은 상세의 2줄로 — 어미도 통일한다"`

---

### Task 6: 0-상태 문구

**Files:**

- Modify: `src/screens/home-child.ts` (스트릭 0)
- Modify: `src/screens/fact-map.ts` (정복 0)
- Modify: `src/styles/app.css` (`.factmap-invite`)

- [ ] **Step 1:** `home-child.ts` — 렌더 전에 `const streak = sprintStreak(days, today)`를 두고 스트릭 줄을 교체:

```ts
<div class="kid-streak">${streak > 0 ? `🔥 ${streak}일 연속` : '오늘 하면 🔥 1일!'}</div>
```

(0은 아이에게 판정으로 읽힌다 — 리뷰 P2-4. 첫날과 리셋 다음날 모두 이 초대 문구가 뜬다. 거짓이 아니다 — 오늘 하면 정말 1일이 된다.)

- [ ] **Step 2:** `fact-map.ts` — `factmap-score` div 위에 초대 줄(0일 때만):

```ts
const invite = fluentCount === 0 ? '<div class="factmap-invite">첫 칸을 채워 볼까?</div>' : ''
```

return 템플릿의 legend와 score 사이에 `${invite}` 삽입. (이 HTML은 리포트에도 실리지만 정복 0일 때뿐이라 부모 화면에서도 무해하다.)

- [ ] **Step 3:** `app.css` — `.factmap-score` 규칙 위에:

```css
.factmap-invite {
  text-align: center;
  font-size: var(--seed-font-size-t4);
  font-weight: 700;
  margin-bottom: var(--seed-dimension-x1);
}
```

- [ ] **Step 4:** `npm test && npm run build` → PASS
- [ ] **Step 5: 커밋** — `git add src/screens/home-child.ts src/screens/fact-map.ts src/styles/app.css && git commit -m "feat: 0-상태를 초대 문구로 — 🔥 0일과 빈 지도가 판정으로 읽히지 않게"`

---

### Task 7: 리포트 구획 + 파괴적 버튼 톤 + 부모 홈 리포트 카드

**Files:**

- Modify: `src/screens/report.ts` (제목·섹션 헤더·reset 클래스)
- Modify: `src/screens/print-sheet.ts` (다시 만들기 버튼)
- Modify: `src/screens/home-parent.ts` (리포트 버튼 라벨+sub)
- Modify: `src/styles/app.css` (`.step.danger`)

- [ ] **Step 1:** `report.ts` 템플릿 — `<h1>주간 리포트</h1>` → `<h1>리포트</h1>`. date div 다음에 `<h2>이번 주</h2>` 삽입. `#confirm` div 앞(공유 버튼 위)에 아무것도 안 넣고, `#export` 버튼 바로 위에 `<h2>데이터 관리</h2>` 삽입. reset 버튼에 danger 클래스:

```ts
${days.length > 0 ? '<button class="step danger" id="reset">모든 기록 지우기</button>' : ''}
```

- [ ] **Step 2:** `print-sheet.ts` — 버튼 줄에서 "다시 만들기"를 오른쪽 끝 + danger로:

```html
<button class="step danger" id="regen" style="margin:0 0 0 auto">다시 만들기</button>
```

- [ ] **Step 3:** `home-parent.ts` — 리포트 버튼을:

```html
<button class="step" id="report">
  리포트
  <small>주간·월간 — 일요일 채점 뒤엔 자동으로 열려요</small>
</button>
```

- [ ] **Step 4:** `app.css` — `.step.done` 블록 아래에:

```css
/* 파괴적 액션은 일반 버튼과 같은 얼굴이면 안 된다(리뷰 P2-2). 확인 배너가
 * 결과를 막고, 이 색이 접근을 늦춘다. 테두리는 그대로 — 텍스트만 critical. */
.step.danger {
  color: var(--seed-color-fg-critical);
}
```

- [ ] **Step 5:** `npm test && npm run build` → PASS
- [ ] **Step 6: 커밋** — `git add src/screens/report.ts src/screens/print-sheet.ts src/screens/home-parent.ts src/styles/app.css && git commit -m "fix: 리포트를 구획하고 파괴적 버튼에 critical 톤을 준다"`

**사람 확인:** 리포트에서 "이번 주 / 월간 / 데이터 관리"의 구획이 읽히는지, 지우기·다시 만들기의 빨간 텍스트 대비.

---

### Task 8: 채점 화면 다듬기 — 날짜·저장 toast

**Files:**

- Modify: `src/screens/grade.ts`

- [ ] **Step 1:** import 줄에 `formatDate`·`toast` 추가 (`../ui`에서). 날짜 두 곳 교체:

```ts
<div class="date">${formatDate(target)} · 틀린 것만 눌러주세요</div>
```

```ts
<p class="date">${formatDate(target)} 문제지가 없어요. 먼저 인쇄해주세요.</p>
```

(target은 `DATE_KEY_RE` 통과값이라 formatDate가 안전하다. 형식 불통과 화면은 target을 아예 안 찍는 기존 동작 유지.)

- [ ] **Step 2:** 저장 핸들러의 putDay 성공 경로에 navigate 직전 한 줄:

```ts
await putDay(updated)
// toast는 body 소속이라 화면 전환(아래 navigate) 뒤에도 3초 떠 있다 — 전환
// 자체가 피드백을 겸하지만 시선이 가는 곳이 아니다(리뷰 P2-5).
toast('채점을 저장했어요', { tone: 'positive' })
navigate(weekdayOf(target) === 0 ? '#/report' : '#/parent')
```

- [ ] **Step 3:** `npm test && npm run build` → PASS
- [ ] **Step 4: 커밋** — `git add src/screens/grade.ts && git commit -m "fix: 채점 화면의 날짜를 사람 말로, 저장에 toast를 단다"`

---

### Task 9: 카피·라벨 통일

**Files:**

- Modify: `src/screens/map.ts` · `src/screens/ebs.ts` (뒤로가기 라벨, EBS 접힘 제목)
- Modify: `src/screens/sprint.ts` (빈 큐 문구)
- Modify: `src/screens/print-sheet.ts` (1장 제목)
- Modify: `src/screens/home-parent.ts` (배너 시맨틱)

- [ ] **Step 1:** `map.ts`·`ebs.ts` — 정상·오류 경로의 `← 홈` 버튼 텍스트를 전부 `← 아이 화면`으로 (4곳). 근거: 두 화면은 두 홈 모두에서 진입하는 공용 화면이고, 부모 홈에 이미 같은 라벨("← 아이 화면")이 있다 — 목적지가 라벨이 된다(리뷰 P2-8). **스프린트의 `← 홈`은 그대로 둔다** — 아이 전용 흐름이라 "홈"이 정확하다.
- [ ] **Step 2:** `ebs.ts` — `<summary>아빠 참고 — 로드맵과 주의사항</summary>` → `<summary>부모 참고 — 로드맵과 주의사항</summary>` (호칭 결정 ③. "부모 →"·"하루치 · 부모"는 이미 결정과 일치라 무변경)
- [ ] **Step 3:** `sprint.ts` — 빈 큐 문구에 행동 지시 추가:

```ts
backOnly(root, '오늘 낼 문제를 만들지 못했어요. 부모님께 보여 주세요.')
```

- [ ] **Step 4:** `print-sheet.ts` — 1장 머리 `<div class="sheet-title">하루치</div>` → `<div class="sheet-title">하루치 · 1장</div>` (2장의 "하루치 · 2장"과 대칭. 클래스·레이아웃 무변경 — 종이 CSS 불변)
- [ ] **Step 5:** `home-parent.ts` — 미채점 배너 div에 `role="button"` 추가:

```ts
<div class="banner seed-callout__root seed-callout__root--tone_warning" id="pending" role="button">…
```

- [ ] **Step 6:** `npm test && npm run build` → PASS
- [ ] **Step 7: 커밋** — `git add src/screens/map.ts src/screens/ebs.ts src/screens/sprint.ts src/screens/print-sheet.ts src/screens/home-parent.ts && git commit -m "fix: 카피 통일 — 아이 화면 라벨, 부모 호칭, 1장 표기, 빈 큐 행동 지시"`

---

### Task 10: 위생 — 토큰·manifest

**Files:**

- Modify: `src/styles/kid.css`
- Modify: `vite.config.ts`

- [ ] **Step 1:** `kid.css` — `.kid-card`의 `background: #fff;` → `background: var(--seed-color-bg-layer-default);` (단일 출처 규칙 — 값 복제 금지)
- [ ] **Step 2:** `vite.config.ts` manifest —

```ts
// SEED bg-layer-basement(light)의 실값. manifest는 CSS 변수를 읽지 못해 복제가
// 불가피하다 — app.css의 body 배경 토큰이 바뀌면 여기도 함께 갱신할 것.
background_color: '#f3f4f5',
theme_color: '#f3f4f5',
```

(기존 `#fafafa`·`#111111`은 SEED 이전 값 — 리뷰 P3-8. 브랜드 색이 확정되면 theme_color를 다시 본다.)

- [ ] **Step 3:** `npm run build` → PASS (manifest는 빌드 산출물로 확인)
- [ ] **Step 4: 커밋** — `git add src/styles/kid.css vite.config.ts && git commit -m "fix: kid-card 배경을 토큰으로, manifest 색을 SEED 값으로 맞춘다"`

---

### Task 11: 문서 갱신 — 결정 기록과 brand.md 정정

**Files:**

- Modify: `docs/design/brand.md` (호칭 결정 반영)
- Modify: `docs/design/ux-review-2026-08-05.md` (결정 기록 절 추가)
- Modify: `docs/superpowers/HANDOFF.md` (라운드 요약 절 추가)

- [ ] **Step 1:** `brand.md` — ① §5의 화자 문장 "이 도구를 만든 아빠"를 "이 도구를 만든 부모"로, "### 아빠에게" 소제목을 "### 부모에게"로. ② §6 용어 사전의 운영자 행을 교체:

```markdown
| 운영자 | **부모** | 보호자, 관리자 | 2026-08-05 사용자 결정 — 화면 호칭 기준. 문서·코드 주석의 "아빠"는 역사 기록으로 유지 |
```

③ §6 아래 근거 문단("아빠를 정본으로 두는 근거…")을 교체:

```markdown
"부모"를 정본으로 두는 근거(2026-08-05 사용자 결정): 특정인이 아니라 역할을
가리켜, 엄마·조부모가 채점하는 날에도 화면이 거짓이 되지 않는다. 화자 규정(§5)은
"이 도구를 만든 부모"로 일반화한다 — 목소리의 온도는 그대로 유지한다.
```

- [ ] **Step 2:** `ux-review-2026-08-05.md` — 문서 끝에 절 추가:

```markdown
## 결정 기록 (2026-08-05)

- P1-1 mood: **설계대로 배선** — `deriveVerticalCount`(기록 연속 해석, plans/2026-08-05-ux-round1.md Task 1)
- P1-2 스프린트 출구: **추가** — 우상단 ✕, 세션 미저장 정책 유지
- P3-1 호칭: **"부모"로 통일** — brand.md 용어 사전 갱신됨
- P3-2 "오늘 어땠어?": **유지** — 실사용(함께 채점 여부) 확인 후 재론
- P3-6 평균/중앙값: **현상 유지 확정** — 아이에게는 평균의 단순함이 맞다
- P2-8: 지도·EBS만 "← 아이 화면"으로, 스프린트 "← 홈"은 유지(아이 전용 흐름)
```

- [ ] **Step 3:** `HANDOFF.md` — 「SEED 디자인 시스템 도입」 절 위에 새 절 추가:

```markdown
## UX 라운드 1 (2026-08-05)

`docs/design/`의 원칙·리뷰·브랜드 문서를 근거로 한 첫 수정 라운드
(계획 `plans/2026-08-05-ux-round1.md`, 리뷰 결정 기록은 리뷰 문서 끝).

- **mood가 드디어 배선됐다.** `deriveVerticalCount`(derive.ts)가 mood 로그에서
  세로셈 문항 수(8|6)를 파생한다 — 😫 3연속 하향·😀 5연속 복구, "연속"은 달력이
  아니라 기록의 연속(재량 결정, 함수 주석 참고). `Settings.verticalCount`는
  기본값 자리로만 남는다. 문제지 조립(print-sheet)과 부모 홈 라벨이 **같은
  파생값**을 쓴다 — 한쪽만 고치면 화면이 거짓말한다
- 스프린트에 조용한 출구(✕)가 생겼다 — 세션 미저장 정책은 그대로다
- 완료(.done=positive 테두리)와 비활성(:disabled)의 시각 어휘가 갈라졌다
- showError가 `(message, detail?)` 2인자가 됐다 — 사람 말 + 작은 기술 상세
- 운영자 호칭은 "부모"로 통일(사용자 결정) — brand.md 용어 사전이 정본
```

- [ ] **Step 4:** `npm run format && npx prettier --check .` → PASS
- [ ] **Step 5: 커밋** — `git add docs/design/brand.md docs/design/ux-review-2026-08-05.md docs/superpowers/HANDOFF.md && git commit -m "docs: 호칭 결정을 brand.md에 반영하고 라운드 결정을 기록한다"`

---

### Task 12: 전체 검증 + PR

- [ ] **Step 1:** `npx prettier --check . && npm test && npm run build` → 전부 PASS (테스트 303 기대)
- [ ] **Step 2:** 소속 불변식 훑기 — `grep -rn "navigate(" src/screens src/ui.ts src/main.ts`로 아이 소속 화면(home-child·sprint·map·ebs·fact-map)에서 부모 해시(`#/parent`·`#/print`·`#/grade`·`#/report`)로 가는 호출이 **0건**인지 확인(삼항연산자 포함 — grade.ts의 `#/report` 분기는 부모 소속이라 예외 아님이 정상)
- [ ] **Step 3:** 종이 불변 확인 — `git diff main...ux-round1 -- src/styles/print.css` → 0줄. `git diff main...ux-round1 -- src/screens/print-sheet.ts`에 게이트(`day.sheet.length === 0`) 변경 없음
- [ ] **Step 4:** `git push -u origin ux-round1` (배포 안 됨 — deploy.yml은 main만 본다)
- [ ] **Step 5:** PR 생성 — squash 정책이라 PR 제목이 곧 main 커밋 메시지다:

```bash
gh pr create --title "feat: UX 리뷰 1라운드 — mood 배선·스프린트 출구·상태 어휘·카피 통일" --body "docs/design/ux-review-2026-08-05.md 반영. 결정 기록은 리뷰 문서 끝 절 참고.

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
```

- [ ] **Step 6:** CI 초록 확인(`gh pr checks --watch`). **머지는 여기서 멈춘다** — 아래 "사람 확인 묶음"을 브라우저·실기기에서 본 뒤 사용자가 결정한다(SEED 도입 때와 같은 선례: 시각 변경은 눈으로 보고 머지)

**사람 확인 묶음(머지 전, dev 서버 `http://localhost:5173/haruchi/`):**

1. 스프린트 ✕ — 탭 즉시 홈, 세션 소실, reveal 도중 탭해도 화면 안 덮임
2. 부모 홈 — 인쇄 전 채점 버튼 disabled 얼굴 / 인쇄 후 done 얼굴 / 문항 수 라벨
3. mood 3일 주입 후 문제지가 세로셈 6으로 나오는지(라벨 일치 포함)
4. 에러 배너 2줄 구조(개발자 도구에서 IndexedDB 차단으로 유발 가능)
5. 리포트 구획·danger 톤 / 채점 저장 toast / 0-상태 문구(시크릿 창)
6. ⌘P 인쇄 미리보기 — 1장 제목 "하루치 · 1장", 이외 종이 변화 없음

## Self-Review 결과

- 리뷰 문서 대비 커버리지: P1 4건(T1·T2·T3·T4·T5), P2 8건(T7·T4·T6·T8·T9), P3 — P3-1(T9·T11) P3-2(결정: 유지) P3-3(T5) P3-4(T7) P3-5(T9) P3-6(결정: 유지) P3-7·P3-8(T10) P3-9(T9의 role="button" — 나머지 aria는 후순위로 리뷰 문서에 남김). 카피 표 13건 전부 태스크에 배정됨
- 타입 일관성: `deriveVerticalCount(days: Day[]): 8 | 6` — T1 정의, T2 두 곳 사용 동일. `showError(message, detail?)` — T5 정의·사용 일치
